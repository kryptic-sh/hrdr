# Maintainer: mxaddict <mxaddict@kryptic.sh>
#
# Auto-generated. Source: https://github.com/kryptic-sh/hrdr
# Edits made directly to a built .apk are not tracked. To change this
# APKBUILD, edit `pkg/alpine/APKBUILD.in` in the upstream repo and let
# `.github/workflows/ci.yml` rebuild on the next tag.
pkgname=hrdr
pkgver={VERSION}
pkgrel=0
pkgdesc="herder — fast, agentic coding harness for OpenAI-compatible models"
url="https://github.com/kryptic-sh/hrdr"
arch="x86_64"
license="MIT"
# !check  — no test suite shipped in the binary tarball.
# !strip  — binary is already stripped by the upstream release build,
#           and abuild's strip step rejects cross-built artifacts.
options="!check !strip"
source="hrdr-v$pkgver-x86_64-unknown-linux-musl.tar.gz::https://github.com/kryptic-sh/hrdr/releases/download/v$pkgver/hrdr-v$pkgver-x86_64-unknown-linux-musl.tar.gz"

package() {
    install -Dm755 "$srcdir/hrdr" "$pkgdir/usr/bin/hrdr"
    install -Dm644 "$srcdir/LICENSE" "$pkgdir/usr/share/licenses/$pkgname/LICENSE"
    install -Dm644 "$srcdir/README.md" "$pkgdir/usr/share/doc/$pkgname/README.md"
}

sha512sums="{SHA512_X86_64}  hrdr-v$pkgver-x86_64-unknown-linux-musl.tar.gz"
